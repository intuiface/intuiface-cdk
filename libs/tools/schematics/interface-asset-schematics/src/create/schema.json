{
    "$schema": "http://json-schema.org/schema",
    "$id": "interface-asset",
    "title": "ng new options schema",
    "type": "object",
    "description": "Initialize a new project",
    "properties": {
        "IAName": {
            "type": "string",
            "description": "The name of the interface asset",
            "x-prompt": "What is your interface asset name ? ",
            "$default": {
                "$source": "argv",
                "index": 0
            }
        }
    }
}