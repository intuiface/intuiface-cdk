/**
 * Behavior to manage gestures.
 */
export class GestureBehavior
{
    //#region Triggers

    /**
     * Gesture detected event.
     */
    public raiseGestureDetected(gestureName: string): void { }

    //#endregion Triggers
}
